"""Recommendation-only structured report builder placeholder.

Planned responsibilities: render the 3–6 line conclusion first, choose exactly
one mutually exclusive BUY/SELL/HOLD/WAIT action, validate output against the
output schema, then render market analysis and reasons, show data/fee confidence,
and clearly state that BUY/SELL values are recommendations only. This module must never create or submit orders.
Implementation starts after the calculation and risk modules.
"""
