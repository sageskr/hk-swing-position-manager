"""Recommendation-only structured report builder placeholder.

Planned responsibilities: validate output against the output schema, render the
required report order, show data/fee confidence, and clearly state that BUY/SELL
values are recommendations only. This module must never create or submit orders.
Implementation starts after the calculation and risk modules.
"""
